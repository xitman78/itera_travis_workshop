landuage: node_js
node_js:
  - stable

cache:
  directories:
  - node_modules



script:
  - npm i
  - npm test
  - npm run build

deploy:
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master